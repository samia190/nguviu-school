# Production Environment Variables for Frontend
# Copy this to .env.production and update with your actual backend URL

# Backend API URL - REQUIRED for production builds
# Set this to your Render backend service URL
VITE_API_URL=https://nguviu-backend.onrender.com

# Note: Vite only includes variables prefixed with VITE_ in the build
# These variables are embedded at BUILD TIME, not runtime
