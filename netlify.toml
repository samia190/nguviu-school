[build]
  base = "nguviu-frontend"
  publish = "dist"
  command = "npm run build:netlify"

# Optional: ensure SPA routing works (you already have _redirects, this is just extra safety)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
