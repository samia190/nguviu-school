# Backend API URL - Points to backend service
# If backend is on same Render app, use relative path
# If backend is separate, use full URL like: https://ksc-backend.onrender.com/api
VITE_API_URL=/api

# Fallback: Uncomment if backend is on separate domain
# VITE_API_URL=https://ksc-backend.onrender.com/api
